# Always save exact versions (avoid surprises)
save-exact=true

# Ensure consistent installs on Vercel and locally
legacy-peer-deps=true

# Don’t auto-install optional native deps that may fail
optional=false

# Enforce using npm v9+ if possible
engine-strict=true
